- if @showtype == "performance"
  = render(:partial => "layouts/performance")
  :javascript
    NOVAHawk.afterOnload = "miqAsyncAjax('#{url_for(:action => @ajax_action, :id => @record)}');"
- else
  = render :partial => @showtype
